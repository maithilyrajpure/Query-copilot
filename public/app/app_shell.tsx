import React from 'react';

/**
 * Application shell. Placeholder for the Query Copilot UI; later tasks replace
 * this with the real query-generation interface.
 */
export const AppShell: React.FC = () => {
  return <div data-test-subj="queryCopilotAppShell">Query Copilot</div>;
};
